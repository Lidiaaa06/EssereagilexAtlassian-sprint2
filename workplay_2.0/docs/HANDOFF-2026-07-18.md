# WorkPlay — Handoff sessione di design 18/07/2026

> Questo file trasferisce a Claude Code il contesto della sessione di design fatta su claude.ai
> (chat "ITSM incom @essereAgile/Ferrari"). Leggilo per intero prima di toccare il codice.
> La fonte viva degli open point è la pagina Confluence indicata sotto: in caso di dubbio, vale quella.

## Contesto in 5 righe

WorkPlay è un plugin Forge/Jira di gamification per il team AMS Atlassian di Ferrari, sviluppato
da 6 tirocinanti ITS Incom con essereAgile (Michele Budri). Siamo a valle dello Sprint 3
(review 17/07). Il codice in questo repo è l'ultima versione dello Sprint 3, PRIMA delle decisioni
di design del 18/07 descritte qui: quindi il codice NON riflette ancora queste decisioni.
Direzione dal PO: semplificare ("gelateria con 2 gusti": base = punti, distintiva = collaborazione/aiuti).

## Decisioni del 18/07 (non ancora implementate)

### Personas (3)
- **Administrator**: solo setup. Due voci in Impostazioni Jira: *Configurazione gruppi* e *Configuration*.
- **Team Leader** (ex "supervisore"): voce condizionale nella app (visibile solo a chi è TL di ≥1 gruppo).
  Eredita dall'admin: Golden Activity, sfide personalizzate, moderazione — con scope limitato al suo gruppo.
- **Developers** (ex "operatori"): usano dashboard, classifica, sfide, hall of fame.
- Rinomina da propagare ovunque: supervisore→Team Leader, operatore→Developers, Golden Ticket→**Golden Activity**.

### Gruppi e gerarchia
- Nuova entità **Gruppo** in KVS: { nome team, teamLeaderId, developers[] }. Sostituisce membri piatti + ruoli.
- Un developer appartiene a **un solo gruppo** (un solo TL, che riceve le sue notifiche). Validazione lato resolver, errore bloccante.
- I gruppi formano un **albero** (un TL può essere developer di un altro gruppo). Vietati i cicli.
- Developer che è anche TL: non rimovibile dal gruppo padre (🔒) finché ha un gruppo suo.
- Notifiche instradate per gruppo; se il TL agisce da developer, vanno al suo TL superiore.
- Eliminazione gruppo: comportamento da definire (vedi open points) — non implementare a caso.

### UI Administrator (Impostazioni Jira)
- **Configurazione gruppi**: pagina unica **master-detail** in **Custom UI + Atlaskit**
  (tree interattiva a sinistra con inline-add che eredita gruppo/TL, pannello dettaglio a destra
  con Modifica/Elimina gruppo). Il resto dell'app resta UI Kit: scelta ibrida per-modulo, stessi resolver via @forge/bridge.
- **Configuration** (ex Punteggi): tab *Punteggi* (punti per azione + stati workflow che assegnano punti,
  finalmente configurabili → chiude il tema "stati Ferrari"), *Stagioni* (durata, pausa, legacy, countdown,
  termina stagione con doppia conferma + audit), *Generale* (da definire: candidati feature-flags per la potatura, progetti attivi).
- **Default all'installazione**: seed della config omogenea in KVS nell'evento lifecycle di install.
- **Stagioni org-wide**: timeline UNICA per l'organizzazione. Nessun calendario per-team. Il modello stagioni resta globale.

### UI app (globalPage) — pattern a sidebar con sotto-voci
Stile "Backbone Work Sync": WorkPlay espanso con Dashboard / Classifica / Sfide / Hall of Fame / 👔 Team Leader.
⚠️ Da verificare: supporto sub-pages nel modulo jira:globalPage Forge; fallback Custom UI con routing.
- **Dashboard operatore**: header identità, azioni rapide (Usa Golden Activity, Segnala aiuto — popup a 3 campi:
  work item, persona, messaggio), 4 KPI, progressione, sfide in corso, feed team.
- **Classifica**: tab Classifica / Classifica Aiuti, medaglie in tabella, colonna Cambio.
- **Area Team Leader** a 3 tab:
  1. *Dashboard*: coda di lavoro A GRIGLIA (checkbox select-all + azioni massive → serve **resolver batch**;
     filtri Tipo/Developer/Stato; raggruppa per tipologia). Azioni: Accetta e affianca (GA), Rivedi/Archivia (anti-farming), Approva/Rifiuta (HoF).
  2. *Andamento team*: grafici stagione/anno → serve **serie storica punti** in KVS (oggi c'è solo lo snapshot giornaliero).
  3. *Il mio team*: gestione scoped (rinomina, add/remove developer) dal suo livello alle foglie;
     sotto-team in sola visualizzazione (proposta da confermare con Michele).

## Riferimenti

- **Figma** (mockup di tutto quanto sopra): fileKey `APJTHFdylzgDhIMpSaWj0w`, pagina "🚀 Prossimi sprint".
  Frame principali: NEW · WorkPlay Dashboard (Operatore) · NEW · WorkPlay Classifica · modali Golden Activity/Segnala aiuto ·
  ADMIN · Configurazione gruppi (tree + dettaglio) + modali Crea/Modifica/errore · ADMIN · Configuration (tab Punteggi, Stagioni) ·
  NEW · Area Team Leader (tab Dashboard, Andamento team, Il mio team). Pagina "📱 Stato attuale (Sprint 3)" = UI ricostruita dal codice.
- **Confluence** (cloudId `9d8d1f27-0cd5-4add-9284-a96b9ffccfd0`, space ICF):
  - ⭐ **Open Points (fonte viva)**: pageId `2663743525` — https://essereagile.atlassian.net/wiki/x/JYDFng
  - Stato dell'arte post Sprint 3: pageId `2664005641` · Sprint 1: `2664136715` · Sprint 2: `2663743499` · Sprint 3: `2664038405`
- **Regole agente già nel repo**: CLAUDE.md / AGENTS.md (piccoli passi, diff prima di applicare, forge lint,
  trappole KVS: niente null, punteggi ×10, chiavi identiche). Restano valide.

## Cosa NON fare

- Non rifinire/ristrutturare l'admin monolitico prima della **potatura guidata** (sessione dedicata con Michele).
- ~~Non implementare l'eliminazione gruppi senza la regola sui figli (open point aperto).~~
  **RISOLTO 19/07/2026**: regole definite e implementate, vedi sotto.
- Non introdurre calendari stagionali per-team: le stagioni sono org-wide.
- ~~Non fare deploy: owner forge login e developer site sono ancora da chiarire (open point).~~
  **RISOLTO 19/07/2026**: l'owner del `forge login` è **atlasadmin@essereagile.com**
  (Account ID `712020:6a7360a2-2cdb-47d6-aa51-8d1f770900fd`). Il deploy su `-e development`
  è autorizzato. Per lo sviluppo quotidiano si usa `forge tunnel`, così le modifiche al
  codice si vedono senza rifare il deploy ogni volta.

## Primi task sensati per Claude Code

1. Leggere la pagina Open Points (via MCP Atlassian se configurato, o chiedere a Michele di incollarla).
2. ~~Verificare il supporto sub-pages di jira:globalPage~~ → **fatto 19/07**: le subpage esistono su
   `jira:adminPage` (non globalPage) e funzionano **solo in Custom UI**. Vedi sotto.
3. Propagare la rinomina Golden Ticket→Golden Activity e supervisore/operatore→Team Leader/Developers (solo naming, no logica).
4. ~~Bozza del modello dati Gruppo in KVS~~ → **fatto 19/07**, vedi sotto.
5. ~~Spike Custom UI + Atlaskit per Configurazione gruppi~~ → **fatto 19/07**: non è più uno spike,
   è completo e collaudato in produzione-dev.

---

# ✅ Sessione 19/07/2026 — Configurazione gruppi COMPLETATA

Versione deployata: **2.15.0** su `-e development`. Tutto verificato dal vivo su dati reali,
non solo compilato.

## Cosa è stato costruito

- **`src/resolvers/gruppi.js`** — modello Gruppo `{ id, nome, teamLeaderId, developers[] }`.
  L'albero è **implicito**: un gruppo H è figlio di G quando il team leader di H compare
  fra i `developers` di G. Nessun campo `parentId`.
- **Pagina admin Custom UI** in `static/gruppi/` — tree master-detail come da Figma (frame `18:9`),
  modale crea/modifica, inline-add che eredita gruppo e TL, avatar, eliminazione.
- **9 resolver** in `index.js`, tutti dietro `isAdminJira` (anche le letture).

## Decisioni di prodotto prese (non ridiscuterle senza motivo)

- **Eliminazione gruppi** — le tre regole volute da Michele:
  1. Un gruppo **con sotto-gruppi non è eliminabile** (pulsante disabilitato + tooltip).
  2. Si eliminano **solo le foglie**, risalendo verso la radice. Niente cancellazione a cascata.
  3. **Doppia conferma** esplicita prima di procedere.
  I developers del gruppo eliminato restano senza gruppo; punti, badge e storico NON vengono toccati
  (vivono su chiavi KVS separate). Il TL resta developer del padre e perde il lucchetto 🔒.
- **Il TL non è developer del proprio gruppo**: guida, non milita. Compare fra i developers del
  gruppo PADRE, ed è esattamente ciò che genera la gerarchia.
- **Una persona guida al massimo un gruppo** (altrimenti il routing delle notifiche non sarebbe univoco).
- **Niente scope `read:email-address:jira`**: mostrerebbe le email scavalcando le impostazioni di
  privacy degli utenti, richiederebbe riconsenso e va giustificato in review Marketplace.
  Per distinguere gli **omonimi** (nel sito ci sono due "Anna Lombardi" e due "Michele Budri",
  account diversi) si usano gli **avatar**, che non costano nulla.
- **Nome organizzazione configurabile** dall'admin (✎ sulla radice, chiave `config-organizzazione`).
  `serverInfo.serverTitle` NON si usa: restituiva "Jira", cioè il nome del prodotto. Il default
  ripiega sul sottodominio del sito e **non viene mai persistito**, così un domani si può migliorare
  il calcolo senza restare bloccati da un valore congelato in KVS.
- **Feature flag `config-gruppi-attivi` = false**. Trigger e notifiche usano ancora
  `membri.js`/`ruoli.js`: **nessun comportamento esistente è cambiato**.

## Vincoli di piattaforma scoperti sul campo (costa caro riscoprirli)

- **`jira:adminPage` ammette UNA SOLA voce top-level** senza `useAsConfig`/`useAsGetStarted`.
  Conseguenza: la voce ora è la pagina nuova, e **il vecchio "WorkPlay Admin" è finito sotto
  Gestisci app → Configura** (`useAsConfig: true`). Funziona identico, cambia solo dove si trova.
- **Le subpage (`pages`/`sections`) funzionano SOLO in Custom UI.** È la vera ragione per cui
  questa pagina non è UI Kit: senza Custom UI non si può avere "WorkPlay → Settings" nella sidebar.
- **Le entry multiple vanno dichiarate in `resources[].entry`.** Referenziare
  `gruppi-resource/modale` senza dichiararla fa servire il file (HTTP 200) ma **senza contesto
  del bridge**: `@forge/bridge` lancia durante l'import e la pagina resta **bianca**, senza errori
  utili. Sintomo diagnostico: `#root` vuoto e zero tag `<style>`.
- **Atlaskit richiede `permissions.content.styles: ['unsafe-inline']`**, perché Emotion inietta
  gli stili inline e la CSP di default li blocca **tutti**. Senza, i componenti si montano ma
  spogli: niente bordi, niente padding, modali invisibili.
- **Una modale Atlaskit non può uscire dall'iframe** della Custom UI: `position: fixed` è relativo
  al viewport dell'iframe. Per coprire la pagina Jira serve `Modal` di `@forge/bridge`, che
  pretende una entry point separata (`modale.html`).
- **Gli avatar Jira sono già consentiti dalla CSP** (`img-src` include il dominio
  `avatar-management--avatars...atl-paas.net`): nessun permesso di egress, nessun rischio per
  "Runs on Atlassian". ⚠️ Il dominio è region-specific (`us-west-2`): non verificato su siti EU.
- **Gli id dei gruppi usano un contatore monotono** (`gruppi-seq`), non `max(esistenti)+1`:
  altrimenti dopo un'eliminazione gli id verrebbero **riusati**, e i futuri riferimenti per gruppo
  (notifiche, area Team Leader) punterebbero al gruppo sbagliato.

## ⚠️ Nota operativa: la Custom UI va COMPILATA

`static/gruppi/dist` è output di build, ed è **quello** che il manifest impacchetta — non i sorgenti.
Modificare `static/gruppi/src/` e lanciare `forge deploy` a mano carica la build **precedente**,
senza errori: si finisce a debuggare codice mai deployato.

**Usa sempre `npm run deploy`** (compila e poi deploya, via hook `predeploy`). Dopo un clone,
`npm run setup`. Dettagli nel [README](../README.md).

---

# ✅ Sessione 19/07/2026 (2ª parte) — Settings: catalogo sfide

Versione deployata: **2.21.0**. Sidebar admin: **WorkPlay → Groups / Settings**.

## Le sfide NON sono più hardcoded

Erano un array `SFIDE` in `sfide.js`. Ora:

- **`src/resolvers/config/sfide-default.json`** — file di configurazione con 20 voci
  (7 giornaliere, 5 settimanali, 7 mensili, 1 feedback) e 4 categorie con punteggio di
  default e limite per periodo. È il **seed**, non la verità a runtime.
- **`src/resolvers/catalogo-sfide.js`** — il catalogo vive in KVS (`catalogo-sfide`,
  `catalogo-categorie`) ed è modificabile dall'admin.
- `sfide-custom.js` è diventato un **adattatore** sul catalogo unico: esiste solo per non
  rompere `index.js` e il pannello admin storico. Nuovo codice → usare `catalogo-sfide.js`.

### ⚠️ NON cambiare le `key` in sfide-default.json

Le sfide accettate dagli utenti sono salvate in `sfide-<accountId>` e referenziano la
**key**. Cambiarne una fa sparire dalle classifiche tutto lo storico di quella sfida.
Le 19 key originali sono state preservate al carattere proprio per questo (il file è
stato **generato dal codice esistente**, non ricopiato a mano).

### Seed: pigro, non lifecycle

Il seed scatta alla prima lettura, non sull'evento di installazione. Ragione: quell'evento
scatta solo all'install e sulle installazioni **già attive** non scatterebbe mai, lasciando
il catalogo vuoto per sempre. Il seed pigro copre entrambi i casi.

È idempotente e usa "mai scritta" (undefined) come segnale, **non** "array vuoto": se
l'admin svuota il catalogo di proposito, non si ripopola da solo.

C'è anche un passo di **aggiornamento**: una categoria aggiunta al file dopo il primo
avvio viene innestata (riconosciuta per `tipo`) senza toccare quelle esistenti, portandosi
dietro le sue sfide solo se non ce n'è già nessuna di quel tipo.

## Funzionalità della scheda Challenges

- Albero per categoria + pannello di dettaglio (master-detail come Groups).
- **✎ sulla categoria**: punteggio di default e limite per periodo. Il limite ha effetto
  immediato su quante sfide un developer può accettare (`accettaSfida` legge da qui).
- **✎ sulla sfida**: modale (Figma 73:140) per titolo, descrizione, punteggio, icona.
  Stessa entry point delle modali dei gruppi, discriminata dal `tipo` nel context —
  una entry in più significherebbe un input Vite, un HTML e una riga di manifest in più.
- **✕**: elimina, con conferma che dichiara la conseguenza (vedi sotto).
- **↺ Ripristina default**: rimette solo le sfide **mancanti**, confrontando per `key`.
  Non sovrascrive mai: una sfida modificata dall'admin resta sua. Le custom non vengono
  toccate né duplicate. Nessuna conferma richiesta: l'azione è puramente additiva.

## Decisioni prese

- **Eliminare una sfida fa sparire i punti di chi l'aveva completata**, anche a stagione
  in corso: `calcolaPuntiSfide` cerca la sfida nel catalogo e, non trovandola, conta 0.
  Conseguenza **accettata consapevolmente** il 19/07; la UI la dichiara prima di confermare.
  Se un domani si volesse evitarla, la strada è salvare `puntiOttenuti` nel record
  dell'utente al momento del completamento, invece di ricalcolarli.
- **Feedback è una categoria vera**: `aiuti.js` legge i punti-aiuto da lì
  (`getCategoria('feedback').puntiDefault`, ripiego 10). Non è un controllo decorativo.
  Non ha ✕ né "aggiungi": è UNA azione con un valore, eliminarla lascerebbe "segnala
  aiuto" senza punteggio.
- **Golden Activity non implementata** (scelta del 19/07). Nota: il mockup la dà
  "1 disponibile/mese" mentre il backend gestisce i golden ticket **per stagione**
  (soglia/max/dotazione). Discrepanza da sciogliere prima di costruirla.
- La scheda **Punteggi** del mockup precedente è stata **assorbita da Challenges**.
  I due controlli che non sono sfide — punti per work item e config Golden Ticket —
  sono in **Generale**, in attesa che il Figma definisca Tab2/Tab3.

## ⚠️ Trappola scoperta sul campo: define duplicati

Aggiungendo un `resolver.define('aggiungiSfidaAdmin')` che **esisteva già** (riga ~584,
gate supervisore, usato dal pannello admin storico), l'INTERO modulo resolver ha smesso
di caricarsi: profilo, classifiche, gadget e trigger giù insieme alla pagina nuova.

`forge lint` **non lo rileva** — è un errore a runtime, quindi il deploy riesce e sembra
tutto a posto. Prima di aggiungere una define:

```bash
grep -o "resolver.define('[A-Za-z]*'" src/resolvers/index.js | sed "s/resolver.define('//;s/'//" | sort | uniq -d
```

Output vuoto = nessun duplicato. I define del catalogo hanno tutti prefisso `catalogo`.

---

# ✅ Sessione 19/07/2026 (3ª parte) — Settings: calendario Season

Versione deployata: **2.27.0**. Scheda **Stagioni rinominata in Season**. Generale invariata
(l'utente ha chiesto di tenerla, si vedrà dopo).

## Calendario stagioni per anno

- **`src/resolvers/config/stagioni-default.json`** — TEMPLATE, non date fisse: `anniDaCreare`
  (3) + i 4 trimestri (mese d'inizio + nome). Le date concrete NON possono stare nel file
  perché l'anno dipende da quando si installa: il seed le genera al primo avvio dall'anno
  corrente. Le stagioni di default sono quindi 4 × 3 anni = 12 (anno install + 2 seguenti).
- **`src/resolvers/catalogo-stagioni.js`** — calendario in KVS (`calendario-stagioni`),
  seed pigro e idempotente (come il catalogo sfide). CRUD + validazioni.
- **UI**: `static/gruppi/src/SeasonsTab.jsx` (albero per anno) + `FormStagione.jsx` (modale).
- **4 resolver** `stagioniGet/Crea/Modifica/Elimina`, prefisso `stagioni` per non collidere.

## ✅ Il calendario ORA governa i punti (rewire fatto 19/07, v2.26.0)

Aggiornamento: `stagione.js` è stato **riscritto** per leggere i confini dal calendario. Il
vecchio ciclo automatico a 2 mesi **non esiste più**. Ora:

- La **stagione corrente** è quella del calendario che contiene "ora".
- La **migrazione dei punti in legacy** scatta quando la stagione in corso **finisce**, cioè
  quando diventa "conclusa" (trigger = la transizione). È esattamente la specifica dell'utente.
- `getGiorniRimanenti`, `getDatiCountdownStagione`, `getCountdownNuovaStagione`, `getStatoStagione`
  ora derivano tutti dal calendario. Le firme sono invariate: countdown-view e getUserStats non
  hanno avuto bisogno di modifiche.

### ⚠️ La trappola che era da evitare (e che è stata evitata)

Al primo avvio col nuovo motore, il rollover avrebbe visto la stagione appena conclusa (2°
trimestre) come "da migrare" e **azzerato i punti di tutti retroattivamente**. Neutralizzato
con un **marcatore** (`stagione-migrata-fino-a` + flag `migrazione-inizializzata`): alla prima
esecuzione segna come già-fatte tutte le stagioni GIÀ concluse, così la migrazione parte solo
quando la stagione ATTUALE finirà. Verificato dal vivo: dopo il deploy il profilo mostrava
ancora "STAGIONE 2" (numero non saltato = nessuna migrazione retroattiva), punti intatti.

Se un domani si tocca `controllaStagione`, ricordare: la migrazione deve restare **una volta
sola per stagione** (guardia sul marcatore) e **mai retroattiva**.

## Decisioni prese

- **Date in UTC**, coerenti col resto dell'app. Un trimestre va da 00:00:00 UTC del 1° giorno
  a 23:59:59 UTC dell'ultimo.
- **Stato calcolato dal backend** (conclusa/corrente/futura) + `giorniRimanenti` (solo per la
  corrente), passati al frontend così la regola non può divergere.
- **La corrente non è eliminabile** (🔒): per chiuderla in anticipo servirà "Termina stagione",
  non ancora costruita. Le stagioni **non possono sovrapporsi** (validato); i giorni scoperti
  sono la pausa.
- **Le concluse sono di sola lettura**: nella treeview solo l'icona 🔍 (niente ✎ né ✕), che
  apre la modale con i campi bloccati; da lì si possono comunque eliminare, con doppia conferma
  (protezione dei dati storici da un clic distratto).
- **Anni: 3** (install + 2). Il mockup ne mostrava 5; ho seguito il testo dell'utente. Si
  cambia in `anniDaCreare`.
- Poiché il calendario governa i reset, **modificare le date della stagione in corso sposta il
  momento della migrazione punti**. Potente: la UI lo dichiara nel banner.

---

# ✅ Sessione 19/07/2026 (5ª parte) — Settings: scheda Workflow

Versione deployata: **2.28.0**. La scheda **Generale è stata sostituita da Workflow**, messa
in **posizione 1** (Workflow / Challenges / Season). Frame Figma 81:2.

## Cosa fa

Regole di monitoraggio: **progetto + stato → trigger → punti**. È ciò che `trigger.js`
consulta per decidere se assegnare punti.

- **`src/resolvers/regole-workflow.js`** — modello in KVS (`regole-workflow`), CRUD + match.
  Il match nel trigger è per **ID dello stato**, non per nome (i nomi cambiano/si ripetono).
  Unicità (progetto, stato). I punti NON stanno sulla regola: vengono dal trigger, uno solo
  per ora (`workitem-completato`), il cui punteggio è `config-punti-per-ticket`.
- **`trigger.js` RISCRITTO**: "è un completamento?" non è più una lista hardcoded, è
  `trovaRegola(progettoKey, statoId)`. L'in-progress/antifarming resta hardcoded per nome
  ('in progress') — il mockup Workflow copre solo l'assegnazione al completamento.
- **UI**: `WorkflowTab.jsx` (regole + card trigger) + `FormRegola.jsx` (modale: picker
  progetto → carica stati del progetto → stato → trigger fisso).
- **4 resolver** `workflowProgetti` / `workflowStatiProgetto` / `workflowRegoleGet` /
  `workflowRegolaCrea|Modifica|Elimina`, tutti isAdminJira.

## ⚠️ REGOLE-ONLY, SENZA SEED (scelta 19/07)

Il trigger ora assegna punti SOLO se una regola copre (progetto, stato). **Sulla vostra
installazione, dopo il deploy 2.28.0 i punti sono FERMI** finché l'admin non crea almeno una
regola. È la scelta esplicita dell'utente ("senza regole, niente punti"), non un bug. Per
riattivarli: Settings → Workflow → Aggiungi regola (es. progetto X, stato Done).

## Decisione RIAPERTA: colonna "Workflow"

L'utente voleva mostrare il nome del workflow del progetto. Ma l'endpoint
`/rest/api/3/workflowscheme/project` richiede **`manage:jira-configuration`** — uno scope
admin MOLTO ampio (lo segnala `forge lint`), sproporzionato per un dato decorativo che al
trigger non serve. Ho quindi **omesso la colonna** e lasciato il campo `workflowNome` vuoto,
senza aggiungere lo scope (così niente riconsenso). La colonna mostra "—".
→ Se si decide di mostrarlo davvero: aggiungere `manage:jira-configuration` al manifest,
riabilitare la fetch in `workflowStatiProgetto` (index.js, c'è il commento), `forge install
--upgrade` + riconsenso admin.

## Golden Ticket → poi spostato in Extra (6ª parte)

La card provvisoria del Golden Ticket in fondo al Workflow è stata poi tolta: la config
è migrata nel tab Extra come "Golden WorkItem" (vedi 6ª parte). Il punteggio del trigger
si modifica dalla matita sulla card "Trigger disponibili" (niente più card doppione).

---

# ✅ Sessione 19/07/2026 (6ª parte) — Extra tab + Workflow per Space (v2.29.0)

Riorganizzazione delle categorie non-challenge e del raggruppamento delle regole.

## Un solo catalogo, due schede (campo `gruppo`)

Feedback è uscito da Challenges ed è finito in un nuovo tab **Extra** (clone grafico di
Challenges). Invece di un secondo catalogo separato, ho aggiunto un campo **`gruppo`** su
ogni categoria: `challenge` (le sfide) o `extra` (Feedback, Golden WorkItem). Il catalogo
KVS resta UNO (`catalogo-sfide`), le due schede filtrano per gruppo. Conseguenze volute:
- **`aiuti.js` intatto**: legge ancora `getCategoria('feedback')` — Feedback è sempre nel
  catalogo, solo taggato `extra`.
- **due file di seed** come da richiesta: `config/sfide-default.json` (ripulito di Feedback,
  categorie taggate `challenge`) e nuovo `config/extra-default.json` (Feedback +
  Golden WorkItem). `catalogo-sfide.js` importa entrambi e li fonde in `DEFAULT`.
- **Migrazione automatica** in `seedCatalogoSeVuoto` (backfill idempotente): le categorie
  già in KVS senza `gruppo` vengono taggate (Feedback→extra, il resto→challenge) e
  `golden-workitem` viene aggiunto come categoria mancante. Verificato dal vivo: Feedback
  è sparito da Challenges e comparso in Extra, personalizzazioni admin preservate
  (default mensili +21, ecc.).

## Golden WorkItem = ex Golden Ticket (rinominato)

"Golden WorkItem" è il **Golden Ticket rinominato** con la terminologia work item di
Jira/JSM (userà anche JSM). È una categoria di **sola config** (`config: true`, niente item):
nel tab Extra è un nodo che, selezionato, mostra soglia / max / dotazione con steppers +
Salva. **I numeri NON sono duplicati**: vivono ancora in `golden-ticket.js`
(getConfigGoldenTicket/setConfigGoldenTicket) — il tab li espone e basta. La logica di
assegnazione/redeem del golden ticket è rimasta identica.

## Workflow: regole raggruppate per Jira Space

Le "Regole di monitoraggio" ora sono **raggruppate per Jira Space**: ogni progetto è
un'intestazione con le sue regole sotto, un "+ Aggiungi trigger" per Space (apre la modale
col progetto **preselezionato**, via `progettoPreset` in FormRegola) e un "+ Aggiungi regola
su un nuovo Space…" in fondo. Il modello dati già ammetteva più regole per progetto
(unicità su progetto+stato): il raggruppamento rende esplicita la possibilità di più trigger
per Space quando ne arriveranno altri.

## Workflow: tolte due card

- **"Punteggio del trigger"** (era un doppione): il +N si modifica ora dalla **matita sulla
  card "Trigger disponibili"** (stepper inline + Salva, salvataggio immediato via
  setConfigPunti). Aggiorna anche le righe delle regole.
- **"Golden Ticket"**: spostata in Extra (sopra).
- Via anche la barra "Salva configurazione" in fondo: ogni modifica (punteggio trigger,
  Golden WorkItem) ora si salva subito dal suo controllo.

## File toccati

Backend: `config/sfide-default.json`, `config/extra-default.json` (nuovo), `catalogo-sfide.js`.
Frontend: `ExtraTab.jsx` (nuovo), `SettingsPage.jsx`, `WorkflowTab.jsx`, `FormRegola.jsx`,
`modale.jsx`. Nessun nuovo resolver: Extra riusa `catalogoGet`/`catalogo*` e i resolver
golden esistenti. Nessun cambio di scope.

---

# ✅ Sessione 19/07/2026 (7ª parte) — Regole per issue type (v2.30.0)

Le regole di monitoraggio ora intercettano **(progetto, issue type, stato)**, non più
solo (progetto, stato). Modello Jira corretto: uno Space ha più issue type, ognuno con un
workflow e quindi i suoi stati.

## Perché — e perché NON serve lo scope pesante

È la stessa `/rest/api/3/project/{key}/statuses` che già usavamo: **restituisce gli stati
GIÀ raggruppati per issue type**. Quindi issue type + stati arrivano da una sola chiamata
col solo `read:jira-work`. La colonna "workflow" che avevo omesso (richiedeva
`manage:jira-configuration`) è **superata**: l'issue type è il livello giusto e gratis.
Quella decisione è chiusa: niente scope nuovo.

## Cosa è cambiato

- **`regole-workflow.js`**: la regola ora ha `issueTypeId` + `issueTypeNome` (via `workflowNome`).
  `trovaRegola(progettoKey, issueTypeId, statoId)`; unicità su (progetto, issue type, stato).
- **`trigger.js`**: legge `issue.fields.issuetype.id` e lo passa a `trovaRegola` (stesso
  issue type per stato-prima e stato-dopo: non cambia con la transizione).
- **`workflowStatiProgetto`** (index.js): non torna più una lista piatta di stati, ma
  `issueTypes: [{id, nome, stati:[…]}]`.
- **`FormRegola.jsx`**: picker Progetto → **Issue type** → Stato (gli stati dipendono
  dall'issue type scelto) → Trigger. Vale in creazione e modifica.
- **`WorkflowTab.jsx`**: colonna issue type prima dello stato.

## ⚠️ Retro-compatibilità wildcard

Le regole create prima di questo campo NON hanno `issueTypeId`. `trovaRegola` le tratta
come **"qualsiasi issue type"** (in UI: lozenge "tutti i tipi"), così i punti non si
fermano. Riaprendole/risalvandole dal picker prendono un issue type specifico. Verificato
dal vivo: picker issue type popolato con i tipi reali (Sub-task/Story/Epic su WA), stato
gated sull'issue type, progetto preselezionato dal "+ Aggiungi trigger".

---

# ✅ Sessione 20/07/2026 (8ª parte) — Stati multipli per regola (v2.31.0)

Lo Stato nella modale è diventato **multiselezione**: una regola può considerare
completamento l'ingresso in UNO QUALSIASI di più stati (es. WON *o* LOST).

## Cosa è cambiato

- **Forma della regola**: `statoId`/`statoNome` (singolo) → **`stati: [{id, nome}]`**
  (array). `trovaRegola` matcha se lo stato del work item è in `stati`.
- **Overlap guard** (`regole-workflow.js`): per lo stesso (progetto, issue type) uno
  stato NON può stare in due regole — altrimenti un cambio di stato darebbe punti da più
  regole. In creazione e in modifica: errore chiaro con il nome dello stato in conflitto.
- **`FormRegola.jsx`**: il Select degli stati ora è `isMulti` (chip). In modifica
  precarica gli stati esistenti.
- **`WorkflowTab.jsx`**: la riga mostra più lozenge di stato; il messaggio di conferma
  eliminazione elenca gli stati.
- **`trigger.js`**: invariato — chiama già `trovaRegola(progetto, issueType, stato)` e il
  match "stato ∈ stati" è dentro `trovaRegola`.

## Retro-compatibilità (doppia, ora)

`statiDi(r)` normalizza sempre ad array: gestisce sia la forma nuova (`stati: []`) sia
quella vecchia a stato singolo (`statoId/statoNome`). Definita sia in `regole-workflow.js`
(backend) sia, localmente, in `WorkflowTab.jsx` e `FormRegola.jsx` (frontend). Una regola
vecchia risalvata dalla modale adotta la forma nuova (i campi `statoId/statoNome` vengono
rimossi). Verificato dal vivo: campo a chip (Done/Waiting), riga con due lozenge stato,
conteggio regole invariato (nessun residuo di test).

---

# ✅ Sessione 20/07/2026 (9ª parte) — Workflow a due famiglie + Decanter (v2.32.0)

Il tab Workflow ora ha una **treeview a due nodi padre**: due FAMIGLIE di trigger.

## Due famiglie

- **⚡ Regole a Evento** — scattano su un evento Jira in tempo reale (`trigger.js`,
  `avi:jira:updated:issue`). *WorkItem Completato* → punti. È tutto ciò che c'era prima.
- **⏳ Regole a Tempo** — maturano nel silenzio: le valuta uno **scheduled trigger**
  (`decanter.js`, `interval: day`). Primo e unico trigger a tempo: *Work Item Decanter*.

Le regole portano `famiglia` ('evento'|'tempo'); `famigliaDi()` fa da default per le
vecchie (→ 'evento'). Migrazione trasparente: le regole esistenti finiscono sotto Evento.

## Work Item Decanter (scelta effetto 20/07)

Quando un work item resta in uno degli **stati sorvegliati** oltre la **soglia in giorni**
(campo `sogliaGiorni` sulla regola, "tempo medio previsto"):
1. **Segnalazione al Team Leader** — stessa coda dell'anti-farming (`aggiungiSegnalazione`,
   flag `DECANTER`, campo `dettaglio`). Visibile nell'admin storico.
2. **Commento sul work item** — best-effort come app (ADF, come golden-ticket), avvisa
   l'assegnatario.
3. **NIENTE malus** — nessun punto tolto. Il sistema segnala, l'umano decide.

`decanter.js` cerca via JQL le issue in (progetto, issue type, stato), misura il tempo-in-
stato dal **changelog** (ultima transizione verso lo stato; fallback: data di creazione),
e con un **dedup** (`decanter-<issueKey>-<statoId>` = istante d'ingresso già segnalato)
evita lo spam notturno e ri-segnala solo se l'issue rientra. Solo assegnatari che sono
**membri** del team. Tetto `MAX_PER_STATO=200` con log se superato (niente troncamento muto).

## Infrastruttura nuova

- `manifest.yml`: modulo `scheduledTrigger` (decanter-scheduled-trigger, interval day) +
  function `decanterHandler`. **Nessuno scope nuovo** (JQL + commento = read/write:jira-work
  già presenti). `forge install --upgrade` non ha chiesto consenso.
- Resolver `decanterEseguiOra` (admin): forza una passata SENZA aspettare lo schedule. Oggi
  NON è collegato a un bottone — pronto se si vuole un "Esegui ora".

## Overlap guard aggiornato

L'unicità stato è ora scoped per **(progetto, issue type, trigger)**: una regola di
completamento e una di decanter possono osservare lo stesso stato (cose diverse), due dello
stesso trigger no. `trovaRegola` (usata dal trigger a evento) matcha SOLO regole
'workitem-completato': gli stati sorvegliati dal Decanter non sono completamenti.

## ⚠️ Verificato / da verificare

Verificato dal vivo: treeview a due famiglie (Evento 3 / Tempo 0), catalogo trigger con
badge famiglia, modale a tempo con campo Soglia + "Stati da sorvegliare" + trigger Decanter,
migrazione regole vecchie → Evento. **NON** esercitato dal vivo il motore decanter (non c'è
modo di forzare lo scheduled trigger on-demand senza un bottone, e non ho creato una regola
a tempo su issue reali per non far partire commenti/segnalazioni senza ok). Il motore resta
inerte finché non esiste una regola a tempo. Logica rivista + `forge lint` pulito.

## Debiti tecnici da tenere d'occhio

- `decanter.js` usa `POST /rest/api/3/search` (classico), **deprecato** da Atlassian a favore
  di `/rest/api/3/search/jql`. Funziona oggi; da migrare prima del Marketplace.
- Il changelog embed via `expand=changelog` può troncarsi su issue con storia enorme: per lo
  stato CORRENTE la transizione d'ingresso è recente, quindi ok in pratica.

---

# ✅ Sessione 20/07/2026 (10ª parte) — Trigger editabili + icone Space (v2.33.0)

Due rifiniture al tab Workflow.

## Trigger editabili

Dalla card "Trigger disponibili", la matita apre un editor con **Nome**, **Descrizione** e
(solo per il trigger a evento) **Punteggio**. Le personalizzazioni stanno in KVS
(`trigger-override`, keyed per trigger); la CHIAVE del trigger NON cambia — trigger.js/
decanter.js continuano a fare il match su quella. `getTriggerCatalogo()` fonde default +
override. Il nome personalizzato si riflette anche nelle righe delle regole (che ora lo
leggono dal catalogo via `nomeTrigger`, non più hardcoded). Resolver `workflowTriggerModifica`.

## Icone dello Space nei picker

Il picker Progetto delle modali (`FormRegola`) mostra l'**avatar del progetto** accanto al
nome (via `formatOptionLabel` + `workflowProgetti` che ora restituisce `avatarUrl` =
`avatarUrls['24x24']`). Verificato dal vivo: l'avatar di "Ti.ch" (bandiera CH) carica.
**Niente permesso `external.images`**: gli avatar sono su dominio Atlassian interno e
passano la CSP dell'iframe di default → l'app resta eleggibile Runs on Atlassian. (Avevo
provato ad aggiungere `external.images` ma dava warning di deprecazione E rischiava
l'eleggibilità: tolto, e funziona lo stesso.)

## Nota cache bundle

Dopo un deploy, il bundle `index-*.js` della pagina può restare in cache del browser mentre
`modale-*.js` si aggiorna: si vede la modale nuova ma la pagina vecchia. Un reload pulito
(o navigare via l'URL diretto della subpage) forza il bundle nuovo.

---

# ✅ Sessione 20/07/2026 (11ª parte) — Audit Log (v2.35.0, deployato e verificato)

Tab **"Audit Log"** in Settings (5° tab, in fondo — Extra NON è un placeholder, quindi
aggiunto, non sostituito). Open Points id-64/65/66. Deployato e verificato dal vivo: evento
config loggato → riletto → filtrato per data → pill ⚙️ + ✓ OK + autore risolto ("Atlas
Admin"). Fail-safe confermato (un errore su setConfigPunti non ha impedito il log della
modifica trigger).

## Modello dati
- Entry KVS: chiave `audit:<ISO-8601 UTC>:<rand4>` (ordinabile per data; regex chiave KVS
  ammette `:`/`.`/`-`, verificato). Valore compatto `{ t, y, a, s?, d:{r?,i?,p?,x?}, o }`,
  mai `null`, mai oggetti Jira interi.
- 3 famiglie `y`: 'trigger' | 'config' | 'tl'. Esito `o`: 'ok'|'skip'|'error'.
- Retention **45 giorni**, pulizia a batch (~500/run) con early-termination (le vecchie
  sono in testa nell'ordine chiave crescente).

## Scheduled trigger CONSOLIDATO (decisione architetturale)
Invece di aggiungere un 2° trigger giornaliero, il modulo esistente del Decanter è diventato
`manutenzione-giornaliera` → `manutenzione.js`, che orchestra **decanter + pulizia audit**
(e in futuro rollover/countdown). Un solo scheduledTrigger, come Open Points id-63.
`decanter.js` non ha più `handler` (resta `eseguiDecanter`, usato da manutenzione + resolver).

## File
- NUOVI: `src/resolvers/audit.js` (logAudit / getAuditLog / pulisciAudit),
  `src/resolvers/manutenzione.js`, `static/gruppi/src/AuditLogTab.jsx`.
- MODIFICATI: `manifest.yml` (scheduledTrigger rinominato + function manutenzioneHandler),
  `src/resolvers/decanter.js` (via handler orfano), `src/resolvers/trigger.js` (log punti
  +/-), `src/resolvers/index.js` (helper `auditConfig`/`auditTl` + 23 resolver strumentati +
  resolver `auditLogGet`), `static/gruppi/src/SettingsPage.jsx` (5° tab).

## Scelte da sapere
- **Skip rumorosi NON loggati**: loggo solo gli eventi che assegnano/tolgono punti (non ogni
  cambio di stato non-monitorato: sarebbe un'esplosione di scritture KVS costose). Da
  estendere in modo mirato se serve un caso specifico.
- **Ordinamento/paginazione**: simple KVS pagina solo per chiave crescente; per il
  newest-first raccolgo la finestra (limitata dalle date + retention) e ordino nel resolver,
  paginando a offset. Semplice e corretto ai volumi previsti; se il log crescesse molto si
  passa a chiavi con timestamp invertito + cursor KVS. Cap a 3000 entry/finestra (avviso in UI).
- **Nessuno scope nuovo.** Serve `forge install --upgrade` al deploy (modulo scheduledTrigger
  rinominato).

## Stato test (20/07)
- ✅ Tab renderizza (filtri, tabella, empty state, nota 45gg); deploy 2.35.0 + install --upgrade ok.
- ✅ Evento **config** loggato e mostrato (modifica trigger) con autore risolto via Jira.
- ✅ Verificati dal vivo più eventi (ripristino default, item eliminato, punteggio, trigger):
  ordinamento newest-first, contatore "N di N voci", autore risolto — tutti ⚙️ config, ✓ ok.
- ✅ `catalogoRipristinaDefault` ora strumentato (v2.37.0): era l'unico resolver lasciato
  fuori; l'utente lo cercava nel log, quindi aggiunto.
- ✅ **Log gruppi resi espliciti (v2.38.0)**: prima "developer rimosso dal gruppo g5" (solo
  id). Ora tutti i log gruppi includono nome developer, nome gruppo e Team Leader (es.
  `developer rimosso: Giovanni Ferrari dal gruppo "Svizzeri" (TL: Michele Budri)`). Helper
  `nomeUtente` (membri → Jira, fail-safe); per elimina/rimuovi si legge il gruppo PRIMA
  dell'operazione. Non testato dal vivo per non toccare gruppi reali dell'org.
- ✅ Colonna "Chi": accountId non-membri risolti via `/rest/api/3/user` (cap 25/lettura).
- ⏳ Da esercitare dal vivo: **#2** completa issue su WPTEST → riga ⚡ (richiede transizione
  Jira con assegnatario membro); **#5** simulazione entry >45gg + pulizia (serve script che
  scriva una chiave `audit:<data vecchia>`). Codice rivisto, logica confermata.
- ✅ **RISOLTO (v2.36.0)**: `setConfigPunti`/`setConfigGoldenTicket` erano gated su
  **isSupervisore**, ma tutta la pagina Settings è admin. Ora sono su **isAdminJira** come il
  resto (autore audit da `context.accountId`, tolto il fetch `/myself`). Verificato: il Salva
  del trigger dà "Trigger aggiornato" (banner verde) invece dell'errore permessi.
  Decisione di prodotto (20/07): **nel menu Settings tutti i valori si modificano da Jira
  admin**. Il ruolo interno (ex supervisore → **TeamLeader**) avrà in futuro un proprio
  Settings ridotto, quando si rifà l'interfaccia con le 2 personas (Developers / TeamLeader).

---

# 📋 STATO CONSOLIDATO DELLA PARTE ADMIN (20/07/2026, v2.39.0)

Sezione riassuntiva: com'è messa OGGI la parte amministrativa, dopo test dal vivo di tutti i
menu e un giro di pulizia del codice. Le sezioni datate sopra restano lo storico dettagliato.

## Superficie admin (Custom UI, `static/gruppi/`)

Voce sidebar Jira **"WorkPlay"** con due pagine:

- **Groups** — albero dell'organizzazione (master-detail): Team Leader + developers, crea/
  rinomina/elimina gruppo, inline add, gerarchia; validazioni lato resolver (un developer in
  un solo gruppo, niente cicli, non elimini un gruppo con figli, ecc.).
- **Settings** — 5 tab, tutti **verificati dal vivo il 20/07**:
  1. **Workflow** — regole di monitoraggio in treeview a due FAMIGLIE (⚡ Evento / ⏳ Tempo).
     Regola = progetto (Space) + issue type + **stati multipli** → trigger. Trigger
     **editabili** (nome/descrizione/punteggio dalla matita). Famiglia Tempo = Work Item
     Decanter con **soglia giorni**. Senza regole a evento → l'app non assegna punti.
  2. **Challenges** — catalogo sfide (categorie fisse giornaliera/settimanale/mensile + item),
     master-detail, "Ripristina default".
  3. **Extra** — Feedback (punti-aiuto) + Golden WorkItem (config golden ticket).
  4. **Season** — calendario stagioni per anno (trimestri): conclusa 🔍 / in corso 🔒 (con
     giorni rimanenti) / futura ✎✕. Governa il reset punti.
  5. **Audit Log** — cronologia eventi (⚡ trigger / ⚙️ config / 👔 TL), filtri data/tipo/
     developer, paginazione 50, retention 45 giorni.

## Backend (`src/resolvers/`)

`index.js` (orchestrazione, ~73 resolver, tutti i gate admin su **isAdminJira**) + moduli di
dominio: `gruppi`, `catalogo-sfide` (Challenges + Extra via campo `gruppo`), `catalogo-
stagioni`, `regole-workflow` (+ catalogo trigger con override), `decanter`, `manutenzione`
(scheduled trigger UNICO: decanter + pulizia audit), `audit`, `antifarming`, `golden-ticket`,
`membri`, `ruoli`, `stagione`, `aiuti`, `halloffame`, `pensieri`, `badges`, `valutazione`.

## Permessi

Tutto Settings + Groups = **amministratore Jira**. Il ruolo interno (ex "supervisore",
d'ora in poi **TeamLeader**) NON governa più i valori di Settings; avrà in futuro una propria
pagina Settings ridotta quando si rifà l'interfaccia con le 2 personas (Developers /
TeamLeader). Le azioni supervisore residue (approvazioni Hall of Fame, badge, ruoli, sfide-
custom) vivono ancora nell'admin legacy `admin.jsx` (config page "Gestisci app → Configura").

## Esito audit di pulizia (20/07)

Codice **pulito**. Corretti: rimosso il resolver orfano `creaGruppoAdmin` (soppiantato da
`creaGruppoCompletoAdmin`); tolti 2 riferimenti stale nei testi ("ex scheda Generale" /
"golden ticket" minuscolo in SettingsPage; commento scheduled trigger in WorkflowTab).
Import inutilizzati, file/branch morti, `resolver.define` duplicati, `console.log` di debug:
**nessuno**.

### ⚠️ Non cablato ma INTENZIONALE (non è "sporco" — è scaffolding di feature previste)

Da NON scambiare per residui e da NON rimuovere: sono agganci pronti per feature decise ma
non ancora esposte in UI.
- `decanterEseguiOra` (resolver) — per il bottone "Esegui ora" del Decanter, ancora da agganciare.
- `setGruppiAttiviAdmin` (resolver) — toggle del flag gruppi (`config-gruppi-attivi`), non ancora esposto.
- `gruppoDelDeveloper` / `gruppoGuidatoDa` (gruppi.js) — routing lungo la gerarchia gruppi.
- `resettaPuntiUtente` (stagione.js) — per "Termina stagione ora".
- `isMembro` (membri.js) — helper del modello membri (documentato in CLAUDE.md).

---

# ✅ Sessione 20/07/2026 (12ª parte) — Inizio rework UI utenti: Dashboard (v2.40.0)

Primo pezzo dell'interfaccia lato utenti (developer/supervisore), da mockup Figma node 6:2.

## La global page "WorkPlay" è passata da UI Kit a Custom UI

Per avere i **sotto-menu in sidebar** (come l'admin con Groups/Settings), il modulo
`jira:globalPage` è ora **Custom UI** con `pages`. Prima sotto-pagina: **Dashboard** (route
`/dashboard`). Classifica / Sfide / Hall of Fame (nel mockup) arriveranno dopo.

- Nuova entry nella build Custom UI ESISTENTE (`static/gruppi`, condivisa con l'admin per non
  duplicare node_modules/bundle): `dashboard.html` → `src/dashboard.jsx` → `HubApp` →
  `DashboardView`. Aggiunta a `vite.config.js` e a `gruppi-resource` (entry `dashboard`).
- Manifest: `globalPage` → `resource: gruppi-resource/dashboard` + `pages:[{Dashboard,
  dashboard}]`; rimosso `render: native`; **rimossa** la resource `hub-resource`.
- Il vecchio hub UI Kit `src/frontend/workplay-hub.jsx` resta su disco come riferimento ma
  **non è più montato** (orfano voluto). Le sue viste (`*-view.jsx`) sono ancora usate dai
  **gadget dashboard** e dai **pannelli issue** (moduli separati, invariati): Classifica /
  Hall of Fame / Pensieri restano raggiungibili da lì.

## Contenuto Dashboard (solo il richiesto)

Header profilo (avatar, nome, ruolo, "🔥 Stagione N") + 3 card: **⏳ Fine stagione** (giorni
rimanenti = countdown), **⭐ Punti stagione** (con breakdown ticket/sfide/valutazioni),
**📋 Ticket chiusi**. Tutto da un solo resolver `getUserStats` (aggiunto `avatarUrl`).
**NON** creati (per scelta): pulsanti "Golden Activity"/"Segnala aiuto" e le card posizione/
progressione/sfide-in-corso/dal-team. Nota "Dashboard in costruzione" in fondo.

Verificato dal vivo: sidebar **WorkPlay ▸ Dashboard**, avatar caricato, countdown 73 giorni,
punti 0 (breakdown), ticket 0. Nessuno scope nuovo; install --upgrade fatto (modulo cambiato).

## Punti all'assegnatario, niente più gate "team" (v2.41.0)

Test punti: WA-1 non dava punti perché il trigger aveva un gate `getMembro` (assegnatario
deve essere nella lista `membri` legacy, scollegata dai Gruppi). Scelta 20/07: **i punti
vanno all'ASSEGNATARIO del work item, chiunque sia**; niente assegnatario → niente punti.
`trigger.js`: rimosso il gate `getMembro`, si usa `issue.fields.assignee.displayName` per i
log/segnalazioni. Verificato dal vivo: completando un work item WA (Story→Done) assegnato,
la Dashboard è salita a **Punti stagione 3 · WorkItem Completati 1**.

## Dashboard: correzioni terminologia + composizione punti (v2.42.0)

- Ruolo: **Developer / Team Leader** (era operatore/supervisore).
- Stagione: **nome dal calendario** (Season tab), non il numero legacy. `getUserStats` ora
  ricava la stagione in corso da `getStagioni()` + `statoStagione()` → `stagioneNome`.
- "Ticket chiusi" → **WorkItem Completati**.
- **Punti stagione = treeview dinamica** della composizione, guidata dal catalogo trigger
  (`getTriggerCatalogo` aggiunto a getUserStats): ogni trigger con la sua somma punti (oggi
  solo WorkItem Completato traccia punti = puntiTicket; Decanter = segnalazione, 0), più
  Sfide e Valutazioni. Nota: Sfide/Valutazioni NON sono ancora nel catalogo trigger — se un
  domani diventano "trigger" servirà il tracking punti per-trigger.

## ✅ Fix Decanter HTTP 410 (v2.43.0)

`decanter.js` usava `POST /rest/api/3/search` — **dismesso** da Atlassian (410). Migrato a
`POST /rest/api/3/search/jql`: cambia la paginazione (non più startAt/total ma
**`nextPageToken`/`isLast`**), `expand: 'changelog'` come stringa. Se il changelog non
arriva, `entrataInStato` ripiega sulla data di creazione (la scansione regge comunque).
Aggiunto un **"▶ Esegui ora"** nel nodo "Regole a Tempo" (admin, → `decanterEseguiOra`) per
forzare la scansione: verificato dal vivo → "Decanter eseguito, 0 segnalati" senza 410.

## ✅ Dashboard developer v2 (mockup 90:2, v2.44.0)

Layout a **due colonne**:
- **Sinistra (~60%)**: card ⏳ Fine stagione (giorni + nome stagione) + card placeholder
  "Spazio disponibile" per i prossimi blocchi (classifica/cambio, progressione badge, sfide,
  feed team).
- **Destra (~40%)**: ⭐ Punti stagione come **treeview espandibile**. Ogni fonte è un nodo;
  cliccandolo si aprono i **singoli eventi** che hanno generato i punti (es. "WA-2 · 20/07
  10:03 · +3"). Fonti: trigger dal catalogo admin (WorkItem Completato con i suoi eventi;
  Decanter = segnalazione) + Sfide + Valutazioni + Aiuti (Feedback).
- **Dati eventi**: `getUserStats` ora restituisce `eventiTrigger` = eventi 'trigger'
  dell'utente nella stagione corrente, letti dall'**Audit Log** (`getAuditLog` con
  developer=me, from=inizio stagione). Nessuna nuova chiamata Jira per evento (issueKey +
  timestamp + punti; il summary del work item si potrà aggiungere se serve).

Verificato dal vivo: layout 2 colonne, nodo WorkItem Completato espanso su WA-2 +3.

### Rifiniture treeview (v2.45.0 / v2.46.0)
- Nodi **chiusi di default** (gli eventi sotto un padre possono essere molti) + due icone in
  alto a destra: **⤢ espandi tutti** / **⤡ chiudi tutti**.
- **Icona del tipo di work item** su ogni evento (🔖 `WA-3 · 20/07 10:55 · +3`):
  `getUserStats` la aggiunge a `eventiTrigger` con UNA sola query (`key in (...)`) invece di N.
- Click sull'**issueKey** → **MODALE NATIVO della issue di Jira** (come il plugin Structure):
  `import { ViewIssueModal } from '@forge/jira-bridge'` →
  `new ViewIssueModal({ context: { issueKey } }).open()`. Apre la vista issue completa
  (descrizione, commenti, tutti i campi) come overlay, **anche da Custom UI in iframe**.
  Verificato dal vivo su WA-3.
  ⚠️ **CORREZIONE a quanto scritto prima**: avevo concluso che il modale nativo non fosse
  richiamabile da Custom UI in iframe — SBAGLIATO. Esiste il pacchetto **`@forge/jira-bridge`**
  (diverso da `@forge/bridge`) con `ViewIssueModal`/`EditIssueModal` ecc. proprio per questo.
  Non serve rendering nativo alla Structure. Rimosso il popup custom di ripiego
  (`IssuePreview.jsx`, resolver `getIssuePreview`, branch `tipo:'issue'` in modale.jsx).
  Nuova dipendenza: `@forge/jira-bridge` in static/gruppi.

### Parte 16 — Pannello "WorkPlay" nell'Activity della issue (jira:issueActivity)
Voce **WorkPlay** nella sezione Activity di ogni work item (accanto a History, Worklogs,
Progeta Timesheet…): mostra gli eventi WorkPlay che hanno coinvolto QUEL work item, così si
verificano dalla history oltre che dall'Audit Log.

- **Modulo**: `jira:issueActivity` (manifest). Prima UI Kit (`render: native`, v2.50–2.55),
  poi **convertito a Custom UI** (v2.57) per la grafica del mockup 100:2 (banner, card,
  chip colorati): niente styling ricco in UI Kit (Box senza background/bordo). Nuova entry
  Custom UI `static/gruppi/activity.html` + `src/activity.jsx` + `ActivityView.jsx`, entry
  Vite `activity`, resource `gruppi-resource/activity`. Rimosso il vecchio
  `src/frontend/workplay-activity.jsx` + resource `wp-activity-resource`.
- **Dati — 3 fonti** aggregate in `getWorkplayActivity`:
  1. **Completamento/Riapertura** (audit `y='trigger'`, filtrato per `d.i` con
     `getEventiIssue`). Il trigger ora **arricchisce** l'audit: `k` (tipo), `st` (stato
     arrivo), `pr` ("KEY — Nome progetto"), `f` (flag antifarming). Eventi vecchi senza
     questi campi → fallback grazioso (tipo dedotto dal segno punti).
  2. **Feedback/aiuto** (`getEventiAiutoTicket` → `aiuti-log-<issue>`): +1, con chi ha dato
     il feedback ("Da …") e descrizione. Pool separato dai punti stagione.
  3. **Decanter** (work item fermo): il decanter ora **logga** un evento audit
     `k='decanter'` (st, gi=giorni, so=soglia). Prima non era tracciato da nessuna parte.
- **Grafica** (`ActivityView.jsx`): banner riepilogo (punti totali · N developer · N eventi),
  una card per evento con avatar (`@atlaskit/avatar`) + nome + azione leggibile + chip tipo
  colorato (⚡ giallo / 🤝 viola / ⏳ grigio / ↩️ rosso) + lozenge punti + orario relativo
  (oggi/ieri/data), righe di dettaglio per tipo, e **vocabolario leggibile** (mappa flag →
  frasi umane, es. `SALTO_IN_PROGRESS` → "passato in Done senza transitare da In Progress").
- **Verificato** (v2.57) su WA-3: banner, card, chip, avatar, nota. Le righe regola/SEGNALATO
  compaiono sui NUOVI eventi (l'evento di WA-3 è pre-arricchimento).

### Parte 17 — Soglia Decanter come DURATA + forzatura manuale nella modale
Le **Regole a Tempo** avevano una soglia in giorni interi. Ora la soglia è una **durata in
stile Jira** (`3h`, `2d`, `1w 2d`; un numero solo = minuti), per gestire granularità sotto il
giorno.
- **Conversione di CALENDARIO** (decisione 20/07): `1w = 7g`, `1d = 24h`. Il Decanter misura
  tempo REALE trascorso, non ore lavorate (quindi NON la conversione lavorativa di Jira 1d=8h).
  Modulo condiviso `durata.js` (`parseDurata`/`formattaDurata`), DUPLICATO identico in
  `src/resolvers/durata.js` e `static/gruppi/src/durata.js` (bundle separati).
- **Dati**: la regola salva `sogliaMinuti` (int) + `sogliaTesto` (per la ri-modifica).
  **Retrocompatibile**: le regole legacy con `sogliaGiorni` valgono ancora (fallback
  ×1440 nel decanter, e in modifica il testo si ricostruisce come "Ng").
- **Decanter**: confronta minuti trascorsi vs `sogliaMinuti`; commento/segnalazione/audit/
  pannello Activity in durata leggibile (es. "2g 3h"). L'audit decanter salva `gi`/`so` in
  MINUTI (il pannello li formatta con `formattaDurata`).
- **Frequenza scan invariata**: resta **giornaliera** (`scheduledTrigger interval: day`).
- **Forzatura manuale** (`FormRegola`, solo in MODIFICA di una regola a tempo): frame con
  **sfondo rosso sfumato** sopra Salva/Annulla, pulsante "▶ Forza manualmente l'esecuzione di
  questo trigger" → conferma inline "Sei proprio sicuro di voler forzare l'esecuzione del
  trigger a tempo?" → invoca il resolver esistente `decanterEseguiOra` (una passata su TUTTE
  le regole a tempo) e mostra l'esito. NB: esiste già anche "▶ Esegui ora" a livello di famiglia.
- **Verificato** (v2.58): campo Soglia con anteprima live ("1d"→"= 1g", "3h"→"= 3h"), fallback
  legacy, frame rosso + messaggio di conferma.
- **Bugfix decanter — gate team + eventi in dashboard** (v2.59–2.60):
  1. Il Decanter aveva ancora il gate `getMembro` (saltava le issue il cui assegnatario non
     era nella vecchia lista `membri`): rimosso, coerente col trigger (scelta 20/07). Ora
     segnala per QUALSIASI assegnatario (`assignee.displayName`). Confermato dal vivo: WA-4
     (assegnata ad AtlasAdmin, non in `membri`) ora viene intercettata.
  2. Gli eventi Decanter (audit `y='trigger'`, `d.k='decanter'`) finivano mescolati sotto
     "WorkItem Completato" nella dashboard. In `getUserStats` ora sono SEPARATI: `eventiTrigger`
     (completamenti) vs `eventiDecanter` (segnalazioni). Nella `DashboardView` il nodo
     "Work Item Decanter" è ora **espandibile** e mostra i work item segnalati con
     "fermo {durata} · nessun punto" (formattaDurata).
- **Tracciamento sempre + interruttore "Ripeti l'azione ad ogni scansione"** (v2.61):
  decisione 20/07. Il Decanter ora **traccia SEMPRE** (logAudit + conteggio) ad ogni scansione
  (giornaliera o forzata) — niente più "0 segnalati" per una issue già segnalata. Le AZIONI
  esterne (segnalazione TL + commento sull'issue) si ripetono ad ogni giro SOLO se la regola
  ha il flag `ripetiOgniGiro` ON; con OFF (default) restano una volta per "soggiorno" nello
  stato (anti-spam commenti). Flag configurabile con un `@atlaskit/toggle` in FormRegola
  (solo famiglia tempo), salvato sulla regola. Il messaggio della forzatura ora dice
  "N rilevati fermi" (non "segnalati"). Verificato dal vivo: WA-4 (già commentata) + WA-5 →
  "2 rilevati fermi su 2 scansionati" con toggle OFF.

### Decisione 20/07 — rework UI utente, una pagina alla volta dai mockup
Le pagine della global page utente vengono ricostruite in Custom UI **una alla volta**, ognuna
a partire dal suo mockup Figma. Fatte finora: **Dashboard** e **Team Leaderboard** (sotto).
Sfide e Hall of Fame NON toccate: l'utente farà prima i mockup dedicati, poi si decide.

### Parte 15 — Team Leaderboard (mockup Figma 6:83)
Seconda sotto-pagina della global page "WorkPlay": la classifica del **proprio team** (gruppo),
NON quella globale. Ogni team gioca la propria classifica; i developer che fanno capo ad altri
Team Leader non compaiono.

- **Routing**: aggiunta la `page` `hub-team-leaderboard` (route `team-leaderboard`) alla
  globalPage nel manifest. STESSO resource `dashboard.html`: `HubApp.jsx` legge il path con
  `view.createHistory()` e monta `DashboardView` o `TeamLeaderboardView` (pattern di App.jsx).
  Nessun nuovo entry html/Vite, nessuno scope nuovo → basta `forge deploy`.
- **Backend**: nuovo resolver `getTeamLeaderboard` (index.js). Il "mio team" = il gruppo di cui
  sono developer (`gruppoDelDeveloper`); se non lo sono ma guido un gruppo, quello
  (`gruppoGuidatoDa`). Righe = i developer del gruppo, punti = stesso totale del profilo/
  `getClassifica` (ticket+sfide+valutazione), più conteggi Task (`ticket-stagione`) e Sfide
  (len `sfide-<id>`) e punti-aiuto (`getNumeroAiuti`) per il 2º tab. "Cambio" via
  `applicaCambioPosizione` con snapshot **PER-GRUPPO** (`classifica-team-<id>` /
  `classifica-team-aiuti-<id>`), altrimenti team diversi si sovrascrivono la fotografia.
- **Decisioni di prodotto (20/07)**: il **Team Leader vede il team che GUIDA** e NON è fra le
  righe (è classificato nel gruppo padre di cui è developer). Chi non è in nessun gruppo →
  **stato vuoto** con messaggio. Contesto stagione (nome + countdown) calcolato PRIMA del
  controllo team, così lo stato vuoto mostra comunque stagione e giorni.
- **Frontend** `TeamLeaderboardView.jsx`: header (titolo + riga stagione + lozenge team con
  nome/numero developer/Team Leader), 2 tab client-side (🏆 XP: colonne XP Tot./Task/Sfide;
  🤝 Aiuti: colonna Punti aiuto), tabella con podio 🥇🥈🥉 tinto, riga "(tu)" in grassetto con
  bordo sinistro, colonna Cambio (▲ verde / ▼ rosso / —), due note a piè di pagina.
- **Verificato dal vivo** (v2.51.0) sul gruppo CUCCIOLONI: entrambi i tab, podio, "(tu)",
  colonne e stato vuoto (per un utente senza team).
- **Avatar profilo Jira** (v2.53.0): nome+avatar risolti con la bulk `risolviNomi` (la stessa
  dell'admin, UNA chiamata per developer+team leader invece di N `nomeUtente`). Reso con
  `<Avatar>` di `@atlaskit/avatar` (fallback iniziali) accanto a ogni developer e al Team Leader
  nell'header. Bonus: nomi più precisi (da Jira, non dai membri legacy).

## Cosa resta aperto

- ~~Pagina "Configuration"~~ → **fatta** come subpage **Settings** (Challenges / Season /
  Generale). Challenges e Season complete; Generale ha controlli in parte spenti.
- ~~Scheda Season~~ → **fatta e collegata al motore punti** (v2.26.0). Resta da costruire
  **"Termina stagione ora"** (chiusura anticipata della corrente, con doppia conferma) e le
  **notifiche countdown** (che non esistono ancora come funzionalità).
- **Area Team Leader** e **dashboard operatore** ridisegnate — non iniziate.
- **Potatura guidata dell'admin monolitico** — sessione dedicata, ancora da fare.
- **Rinomina** Golden Ticket→Golden Activity, supervisore→Team Leader, operatore→Developers.
- **Stati del workflow configurabili**: è il tema "stati Ferrari". Da decidere da dove
  leggere la lista, dato che ogni progetto Jira ha i suoi.
- **Permessi di Settings**: `setConfigPunti` e `setConfigGoldenTicket` sono protetti da
  `isSupervisore`, non da `isAdminJira`. Un admin Jira che non sia supervisore riceve
  "Non hai i permessi" al salvataggio. Da allineare quando si decide il modello permessi.
- **Golden Activity** nel catalogo (vedi discrepanza per-mese / per-stagione sopra).
- **Accensione del flag gruppi**: spostare i consumatori (trigger, notifiche) da `membri.js`/`ruoli.js`
  al modello Gruppo. La **Team Leaderboard** è il primo consumatore live del modello Gruppo (legge
  i gruppi a prescindere dal flag); trigger e punti restano invece su assignee/`membri.js`.
- **Il nome organizzazione è modificabile inline sull'albero**: quando esisterà "Configuration",
  valutare se spostarlo lì (è un'impostazione globale dentro una schermata sui gruppi).
